import { createTheme } from '@mui/material';

const Theme = createTheme({
    palette: {
        // mode: 'dark',
        primary: {
          main: '#228B22',
        },
        secondary: {
          main: '#f50057',
        },
        
    },

  });

export default Theme;